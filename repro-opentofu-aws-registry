terraform {
  required_version = ">= 1.5.7"

  required_providers {
    aws = {
      # 👇 deliberately use the full Terraform registry hostname
      source  = "registry.terraform.io/hashicorp/aws"
      version = "~> 5.60"
    }
  }
}

provider "aws" {
  region = "eu-west-1"
}
